# Copy to .env for local development. Never commit the filled-in file.
# Note that Create React App inlines REACT_APP_* values into the built
# bundle, so anything here is publicly readable. Never put a secret in this
# file, only public endpoints.

# Base URL of the ParkingBae Flask API
REACT_APP_BACKEND_URL=
